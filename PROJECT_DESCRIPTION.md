# 📋 Описание проекта SMM Travel Bot

## 🎯 Назначение проекта

Telegram-бот для автоматической генерации и публикации контента о путешествиях с использованием OpenAI API (ChatGPT для текста, DALL-E для изображений).

## ⚙️ Основные функции

### 1. Генерация контента
- **Генерация постов**: Автоматическое создание текстовых постов о путешествиях
- **Генерация изображений**: Создание иллюстраций с помощью DALL-E 3
- **Кастомные темы**: Генерация постов на заданную тему
- **Анализ идей**: Анализ предложенных тем для постов

### 2. Управление публикациями
- **Отложенная публикация**: Создание постов с последующей публикацией
- **Немедленная публикация**: Генерация и публикация в один шаг
- **Управление очередью**: Просмотр и управление неопубликованными постами

### 3. Автоматизация
- **Планировщик**: Автоматическая публикация по расписанию (ежедневно, еженедельно)
- **Очередь постов**: Использование существующих неопубликованных постов перед генерацией новых

### 4. Статистика и управление
- **Статистика**: Отслеживание количества опубликованных/неопубликованных постов
- **Просмотр постов**: Просмотр всех постов с фильтрацией по статусу
- **Диагностика БД**: Проверка состояния базы данных

## 📁 Структура файлов и их назначение

### Основные файлы Python

#### `bot.py` (Основной файл бота)
**Назначение**: Основная логика Telegram-бота
**Функции**:
- Обработка команд пользователя (`/start`, `/generate`, `/publish`, и т.д.)
- Управление взаимодействием с пользователями
- Интеграция с OpenAI для генерации контента
- Управление базой данных постов

**Основные команды**:
- `/generate` - Генерация нового поста
- `/generate_custom [тема]` - Генерация поста на заданную тему
- `/publish` - Публикация последнего неопубликованного поста
- `/publish_now` - Генерация и немедленная публикация
- `/list_posts` - Список неопубликованных постов
- `/all_posts` - Список всех постов
- `/stats` - Статистика бота
- `/fix_published_posts` - Исправление неправильно помеченных постов
- `/db_diagnostic` - Диагностика базы данных
- `/recommend` - Рекомендации по контенту
- `/analyze [тема]` - Анализ идеи для поста
- Команды расписания: `/schedule_status`, `/schedule_daily`, `/schedule_weekly`, `/schedule_start`, `/schedule_stop`

#### `api_server.py` (API сервер)
**Назначение**: HTTP API для интеграции с внешними сервисами (Make.com и др.)
**Функции**:
- REST API endpoints для генерации постов
- Публикация готового контента
- Получение списка неопубликованных постов
- Запуск FastAPI сервера (Uvicorn)

**API Endpoints**:
- `POST /api/generate` - Генерация поста через API
- `POST /api/publish` - Публикация существующего поста
- `POST /api/publish_content` - Публикация готового контента
- `GET /api/posts/unpublished` - Список неопубликованных постов
- `GET /api/status` - Статус API

#### `generators.py` (Генераторы контента)
**Назначение**: Генерация текста и изображений с помощью OpenAI
**Функции**:
- `generate_post_text()` - Генерация текста поста
- `generate_image_prompt()` - Генерация промпта для изображения
- `generate_image()` - Создание изображения через DALL-E
- `download_image()` - Скачивание изображения
- `generate_complete_post()` - Полная генерация поста (текст + изображение)
- `generate_content_recommendations()` - Генерация рекомендаций по контенту
- `generate_topic_ideas()` - Генерация идей для тем
- `analyze_post_idea()` - Анализ предложенной идеи

#### `models.py` (Модели базы данных)
**Назначение**: Работа с базой данных (SQLAlchemy ORM)
**Функции**:
- Определение моделей данных (`Post`, `Schedule`)
- Инициализация базы данных
- CRUD операции с постами:
  - `add_post()` - Добавление поста
  - `get_unpublished_posts()` - Получение неопубликованных постов
  - `mark_post_published()` - Отметка поста как опубликованного
  - `fix_null_is_published()` - Исправление NULL значений
  - `get_posts_diagnostic()` - Диагностика постов
- Управление расписанием:
  - `get_active_schedule()` - Получение активного расписания
  - `update_schedule_last_run()` - Обновление времени последнего запуска

#### `scheduler.py` (Планировщик)
**Назначение**: Автоматическая публикация постов по расписанию
**Функции**:
- `setup_scheduler()` - Настройка планировщика (APScheduler)
- `start_scheduler()` - Запуск планировщика
- `stop_scheduler()` - Остановка планировщика
- `scheduled_post_job()` - Задача публикации поста
- `publish_post_to_telegram()` - Публикация поста в Telegram группу
- `add_default_schedule()` - Добавление расписания по умолчанию

#### `config.py` (Конфигурация)
**Назначение**: Управление конфигурацией приложения
**Функции**:
- Загрузка переменных окружения из `.env`
- Хранение всех настроек (Telegram, OpenAI, БД, API)
- Валидация конфигурации

### Конфигурационные файлы

#### `requirements.txt`
**Назначение**: Список зависимостей Python проекта
**Содержит**: Все необходимые пакеты (aiogram, openai, fastapi, sqlalchemy, и др.)

#### `.env`
**Назначение**: Переменные окружения (не коммитится в Git)
**Содержит**:
- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота
- `OPENAI_API_KEY` - Ключ OpenAI API
- `TELEGRAM_GROUP_ID` - ID группы для публикации
- `ADMIN_ID` - ID администратора бота
- `DATABASE_URL` - URL базы данных
- `API_HOST`, `API_PORT` - Настройки API сервера

#### `travel-bot-api.service`
**Назначение**: Systemd unit файл для запуска бота как системного сервиса
**Использование**: Автозапуск бота на Linux сервере

### Документация

#### `README.md`
**Назначение**: Основная документация проекта
**Содержит**: Описание проекта, установку, настройку, использование

#### `BOT_TROUBLESHOOTING.md`
**Назначение**: Руководство по диагностике и решению проблем
**Содержит**: Типичные ошибки, команды для диагностики, решения проблем

### База данных

#### `travel_bot.db`
**Назначение**: SQLite база данных (создается автоматически, не коммитится в Git)
**Содержит**:
- Таблица `posts` - Хранит все посты (id, topic, content, image_url, is_published, и т.д.)
- Таблица `schedules` - Хранит расписания публикаций

## 🖥️ Размещение на сервере

### Структура директорий на сервере

```
/root/smm-travel-bot/
├── api_server.py           # API сервер
├── bot.py                  # Основной файл бота
├── config.py               # Конфигурация
├── generators.py           # Генераторы контента
├── models.py               # Модели БД
├── scheduler.py            # Планировщик
├── requirements.txt        # Зависимости
├── travel-bot-api.service  # Systemd unit файл
├── .env                    # Переменные окружения (НЕ коммитить!)
├── travel_bot.db           # База данных (создается автоматически)
├── logs/                   # Логи (создается автоматически)
│   └── bot.log
├── images/                 # Изображения постов (создается автоматически)
└── venv/                   # Виртуальное окружение Python
```

### Минимальный набор файлов на сервере

**Обязательные файлы (9 файлов):**
1. `bot.py` - Основной файл бота
2. `api_server.py` - API сервер
3. `config.py` - Конфигурация
4. `generators.py` - Генераторы
5. `models.py` - Модели БД
6. `scheduler.py` - Планировщик
7. `requirements.txt` - Зависимости
8. `travel-bot-api.service` - Systemd service
9. `.env` - Переменные окружения

**Автоматически создаваемые:**
- `travel_bot.db` - База данных
- `logs/bot.log` - Логи
- `images/` - Папка с изображениями
- `venv/` - Виртуальное окружение (создается командой `python -m venv venv`)

### Запуск на сервере

#### 1. Установка зависимостей
```bash
cd /root/smm-travel-bot
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### 2. Настройка .env
Создайте файл `.env` с необходимыми переменными окружения.

#### 3. Установка systemd сервиса
```bash
sudo cp travel-bot-api.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable travel-bot-api
sudo systemctl start travel-bot-api
```

#### 4. Проверка статуса
```bash
sudo systemctl status travel-bot-api
```

#### 5. Просмотр логов
```bash
sudo journalctl -u travel-bot-api -f
# или
tail -f logs/bot.log
```

### Обновление кода на сервере

1. Скопируйте обновленные файлы на сервер (через FTP/SSH/SCP)
2. Перезапустите сервис:
   ```bash
   sudo systemctl restart travel-bot-api
   ```
3. Проверьте статус:
   ```bash
   sudo systemctl status travel-bot-api
   ```

### Порты и сеть

- **API сервер**: Порт `8000` (настраивается в `.env`)
- **База данных**: SQLite (файл `travel_bot.db`)
- **Telegram**: Использует polling (не требует открытых портов)

## 🔄 Поток работы

1. **Генерация поста**: Пользователь вызывает `/generate` → бот генерирует текст и изображение → сохраняет в БД с `is_published=False`
2. **Публикация**: Пользователь вызывает `/publish` → бот берет последний неопубликованный пост → публикует в группу → отмечает как опубликованный
3. **Автоматическая публикация**: Планировщик по расписанию → проверяет неопубликованные посты → публикует или генерирует новый → отмечает как опубликованный

## 🔐 Безопасность

- Все секретные данные хранятся в `.env` (не коммитится в Git)
- API endpoints можно защитить через API ключ
- Доступ к боту ограничен `ADMIN_ID`
- База данных защищена правильными правами доступа

## 📊 Архитектура

```
┌─────────────┐
│   Telegram  │
│     Bot     │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│   bot.py    │─────▶│  generators  │
│             │      │     .py      │
└──────┬──────┘      └──────┬───────┘
       │                    │
       │                    ▼
       │            ┌──────────────┐
       │            │  OpenAI API  │
       │            └──────────────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│ models.py   │─────▶│ travel_bot   │
│             │      │     .db      │
└──────┬──────┘      └──────────────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐
│ api_server  │─────▶│   FastAPI    │
│     .py     │      │   (Uvicorn)  │
└─────────────┘      └──────────────┘
       │
       ▼
┌─────────────┐
│  scheduler  │
│     .py     │
└─────────────┘
```

---

**Версия**: 1.0.0  
**Последнее обновление**: Декабрь 2025
