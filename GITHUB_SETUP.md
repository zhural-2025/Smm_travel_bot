# 📤 Инструкция по размещению проекта на GitHub

## Шаг 1: Подготовка проекта

Убедитесь, что файл `.gitignore` настроен правильно (уже настроен).

## Шаг 2: Инициализация Git репозитория

```bash
# Инициализация Git
git init

# Добавление всех файлов (кроме тех, что в .gitignore)
git add .

# Создание первого коммита
git commit -m "Initial commit: SMM Travel Bot project"
```

## Шаг 3: Создание репозитория на GitHub

1. Откройте https://github.com
2. Войдите в свой аккаунт
3. Нажмите кнопку **"New"** (или значок `+` → **"New repository"**)
4. Заполните форму:
   - **Repository name**: `smm-travel-bot` (или любое другое название)
   - **Description**: "Telegram-бот для автоматической генерации контента о путешествиях"
   - **Visibility**: Выберите **Public** или **Private**
   - **НЕ** ставьте галочки "Initialize with README", "Add .gitignore", "Add license"
5. Нажмите **"Create repository"**

## Шаг 4: Подключение к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
# Добавление remote репозитория (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/smm-travel-bot.git

# Переименование основной ветки в main (если нужно)
git branch -M main

# Отправка кода на GitHub
git push -u origin main
```

## Шаг 5: Создание .env.example (важно!)

Создайте файл `.env.example` с примером переменных окружения (без реальных значений):

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
TELEGRAM_GROUP_ID=-1001234567890
ADMIN_ID=123456789
DATABASE_URL=sqlite:///./travel_bot.db
API_HOST=0.0.0.0
API_PORT=8000
```

Этот файл можно коммитить в Git, чтобы другие разработчики знали, какие переменные нужны.

## Шаг 6: Обновление README (опционально)

Проверьте, что `README.md` содержит:
- Описание проекта
- Инструкции по установке
- Информацию о необходимых переменных окружения

## Альтернатива: Использование GitHub Desktop

Если предпочитаете графический интерфейс:

1. Установите [GitHub Desktop](https://desktop.github.com/)
2. Откройте GitHub Desktop
3. File → Add Local Repository
4. Выберите папку проекта
5. Publish repository → Выберите название и видимость
6. Нажмите Publish

## Проверка

После выполнения всех шагов:

1. Откройте репозиторий на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что `.env`, `travel_bot.db`, `venv/`, `images/` НЕ попали в репозиторий

## Важные замечания

⚠️ **БЕЗОПАСНОСТЬ:**
- Никогда не коммитьте файл `.env` с реальными токенами
- Проверьте `.gitignore` перед первым коммитом
- Если случайно закоммитили `.env`, немедленно смените все токены

✅ **Что должно быть в репозитории:**
- Все `.py` файлы
- `requirements.txt`
- `README.md`
- `PROJECT_DESCRIPTION.md`
- `PROJECT_OVERVIEW.md`
- `BOT_TROUBLESHOOTING.md`
- `.gitignore`
- `travel-bot-api.service`
- `.env.example` (пример, без реальных данных)

❌ **Чего НЕ должно быть:**
- `.env` (с реальными токенами)
- `travel_bot.db`
- `venv/`
- `images/`
- `logs/`
- `__pycache__/`

