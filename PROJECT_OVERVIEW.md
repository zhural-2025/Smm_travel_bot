# 🎯 Обзор проекта SMM Travel Bot

## Назначение

Telegram-бот для автоматической генерации и публикации контента о путешествиях с использованием OpenAI API (ChatGPT + DALL-E).

## Основные функции

### Генерация контента
- **Автоматическая генерация постов**: Создание текстовых постов о путешествиях с помощью ChatGPT
- **Генерация изображений**: Создание иллюстраций к постам через DALL-E 3
- **Кастомные темы**: Генерация постов на заданную тему пользователем
- **Рекомендации**: Получение рекомендаций по контенту, идеи для тем, анализ предложенных идей

### Управление публикациями
- **Отложенная публикация**: Создание постов с последующей публикацией через команду `/publish`
- **Немедленная публикация**: Генерация и публикация в один шаг через `/publish_now`
- **Очередь постов**: Просмотр списка неопубликованных постов, выбор для публикации
- **Просмотр постов**: Просмотр всех созданных постов с фильтрацией по статусу

### Автоматизация
- **Планировщик публикаций**: Автоматическая публикация постов по расписанию (ежедневно или еженедельно)
- **Умная очередь**: Использование существующих неопубликованных постов перед генерацией новых
- **Гибкое расписание**: Настройка времени публикации и дней недели

### Статистика и управление
- **Статистика бота**: Отслеживание количества опубликованных и неопубликованных постов
- **Диагностика БД**: Проверка состояния базы данных, исправление некорректных записей
- **Управление постами**: Просмотр, редактирование статуса, исправление ошибочно помеченных постов

## Архитектура

```
┌─────────────┐
│   Telegram  │ ←─── Пользовательские команды
│     Bot     │
└──────┬──────┘
       │
       ├──→ bot.py ──────→ generators.py ──────→ OpenAI API
       │                      (ChatGPT, DALL-E)
       │
       ├──→ models.py ────→ travel_bot.db (SQLite)
       │                      (Хранение постов)
       │
       ├──→ api_server.py ─→ FastAPI/Uvicorn (Порт 8000)
       │                      (REST API для внешних сервисов)
       │
       └──→ scheduler.py ──→ APScheduler
                              (Автоматическая публикация)
```

## Ключевые компоненты

**bot.py** - Обработка команд Telegram, управление пользователями  
**generators.py** - Генерация текста и изображений через OpenAI  
**models.py** - Работа с базой данных (SQLAlchemy)  
**api_server.py** - HTTP API для интеграций  
**scheduler.py** - Планировщик автоматической публикации  

## Поток работы

1. **Генерация**: `/generate` → Генерация текста и изображения → Сохранение в БД
2. **Публикация**: `/publish` → Публикация в группу → Отметка как опубликованный
3. **Автоматизация**: Планировщик → Проверка очереди → Публикация по расписанию

## Технологии

- **Python 3** - Основной язык
- **Aiogram 3** - Telegram Bot Framework
- **OpenAI API** - ChatGPT + DALL-E 3
- **FastAPI** - HTTP API сервер
- **SQLAlchemy** - ORM для работы с БД
- **APScheduler** - Планировщик задач
- **SQLite** - База данных

---

📖 **Подробное описание**: См. `PROJECT_DESCRIPTION.md`

